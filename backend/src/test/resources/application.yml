spring:
  datasource:
    url: jdbc:mysql://localhost:3306/molview_test
    username: root
    password: password
  application:
    name: molview-backend
  flyway:
    create-schemas: false
    default-schema: molview_test
  ai:
    openai:
      api-key: foo_fake_key
      base-url: https://api.openai.com
      chat.options.model: gpt-4o-mini